declare const AuthereumSigner_base: any;
export default class AuthereumSigner extends AuthereumSigner_base {
    private authereum;
    private config;
    provider: any;
    constructor(config: any);
    getAddress(): Promise<string>;
    signMessage(message: string): Promise<string>;
    private sign;
    sendTransaction(transaction: any): Promise<any>;
    sendTransactionBatch(transactions: any[]): Promise<any>;
    estimateGasBatch(transactions: any[]): Promise<number[]>;
}
export {};
